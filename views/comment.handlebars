<div>Yo soy comments</div>
<div class="row">
  <div class="text-center">
    <h2 class="text-2xl font-bold  mb-4 border-b-4 border-b-gray-300">Hello, {{logged_user_name}}!</h2>
    <div class="text-3xl font-bold mb-4">Find Post details below!</div>
    <div class="border-2 border-indigo-700 bg-indigo-700 text-white py-2 w-auto font-semibold rounded-t-md"><h2>{{title}}</h2></div>
    <hr>
      <div class="w-auto p-6 shadow-lg bg-gray-400 rounded-b-md">
      <p class="mb-3">{{description}}</p>
      <p class="mb-3">Created by {{user.name}} on {{format_date createdAt}}</p>
      <div>
        <div class="row">
  <div class="col-auto">
    <h2 class="text-2xl font-bold  mb-10 border-b-4 border-b-gray-300"></h2>
  </div>
</div>
{{!-- from here --}}
  <div class="flex justify-center items-center">
    <div class="w-96 p-6 shadow-lg bg-gray-500 rounded-md m-auto">
      <h1 class="text-3xl block text-center font-semibold"><i class="fa-solid fa-user"></i>Enter Comment</h1>
      <form class="form new-comment-form">
        <input type="hidden"id="postId" value="{{id}}"/>
        <input type="hidden"id="userId" value="{{logged_user_id}}"/>
        <div class="form-group mt-5">
          <label for="comment-description" class="block text-base mb-1 ">Description:</label>
          <textarea class="form-input border h-40 w-full text-base ps-2 py-1 focus:outline-none focus:ring-0 focus:border-gray-600" id="comment-description" name="comment-description"></textarea>
        </div>
        <div class="form-group mt-5">
          <button type="submit" class="border-2 border-indigo-700 bg-indigo-700 text-white py-1 px-5 w-full rounded-md hover:bg-transparent hover:text-indigo-700 font-semibold">Submit Comment</button>
        </div>
      </form>
    </div>
  </div>
      </div>
    </div>
  </div>
</div>
<script src="/js/comment.js"></script>